import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    
    areaButton:{
        width:"70%",
        height: 30,
    },
    textButton:{
        fontSize: 16,
        color: '#FFFFFF',
    },

})

export default styles;